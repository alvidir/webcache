version: '3.8'

services:
    redis:
        container_name: redis
        image: docker.io/redis:latest
        restart: always
        ports:
            - "6379:6379"
        networks: 
            - cache

    # webcache:
    #     container_name: webcache-server
    #     image: localhost/alvidir/webcache:0.1.0
    #     restart: always
    #     volumes:
    #         - .config:/app/.config
    #     networks: 
    #         - cache
    #     depends_on:
    #         - redis
    #     env_file:
    #         - .env

networks:
    cache:
        name: cache
        driver: bridge